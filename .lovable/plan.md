
# Визуальная оптимизация под 3 режима

## Что нужно исправить

После удаления режима "Ввод" осталось 3 режима, но сетки и тексты рассчитаны на 4. Нужно адаптировать.

### 1. Страница Learn (`src/pages/Learn.tsx`)

**Строка 65** — сетка кнопок режимов внутри карточки темы: заменить `grid-cols-2` на `grid-cols-3`, чтобы все 3 кнопки стояли в один ряд без пустого места.

### 2. Главная страница (`src/pages/Index.tsx`)

- **Строка 277** — секция "Режимы обучения": заменить `lg:grid-cols-4` на `lg:grid-cols-3` и центрировать сетку (`max-w-4xl mx-auto`), чтобы 3 карточки выглядели сбалансированно.
- **Строки 122-124** — текст в feature pills: заменить "4 режима изучения" на "3 режима изучения" и "4 τροποι μαθησης" на "3 τροποι μαθησης".
